# 基于深度学习的无人机航拍小目标检测算法研究

本项目针对无人机航拍场景下的小目标检测问题，基于 YOLO11 系列模型，在 VisDrone 2019 数据集上进行训练与优化；提供 **Ultralytics 训练脚本**、**基于 `results.csv` 的指标出图脚本**，以及 **PyQt6 桌面检测应用**，支持图片、视频、摄像头检测与训练指标展示。

---

## 一、项目概述

无人机航拍图像具有目标尺度小、密集分布、多尺度混合等特点，传统检测算法难以取得理想效果。本项目采用 Ultralytics YOLO11 框架，结合 VisDrone 数据集进行训练，实现了对行人、车辆等 10 类交通相关目标的高效检测，并配套开发了基于 PyQt6 的桌面应用，便于模型验证与日常使用。

---

## 二、数据集

### 2.1 数据集简介

本项目使用 **VisDrone 2019-DET** 数据集，由天津大学机器学习与数据挖掘实验室 AISKYEYE 团队发布，对应 ICCV 2019 "Vision Meets Drone" 挑战赛，是面向无人机视角目标检测的大规模基准数据集。

### 2.2 数据规模

| 子集 | 图像数量 | 说明 |
|------|----------|------|
| 训练集 (train) | 6,471 张 | 用于模型训练 |
| 验证集 (val) | 548 张 | 用于超参数调优与模型选择 |
| 测试集 (test-dev) | 1,610 张 | 含标注，可进行论文实验与结果发布 |

- **总标注框数量**：超过 260 万个边界框  
- **图像来源**：288 个视频片段 + 10,209 张静态图像  
- **图像格式**：JPEG (.jpg)

### 2.3 目标类别

数据集包含 **10 个预定义类别**：

| 类别ID | 英文名称 | 中文名称 |
|--------|----------|----------|
| 0 | pedestrian | 行人 |
| 1 | people | 人群 |
| 2 | bicycle | 自行车 |
| 3 | car | 小汽车 |
| 4 | van | 面包车 |
| 5 | truck | 卡车 |
| 6 | tricycle | 三轮车 |
| 7 | awning-tricycle | 带篷三轮车 |
| 8 | bus | 公交车 |
| 9 | motor | 摩托车 |

### 2.4 数据集特点

- **地理位置**：中国 14 个不同城市，相距数千公里  
- **场景类型**：城市、乡村等多种环境  
- **小目标特性**：无人机航拍高度较高，目标在图像中占据像素较少，大量目标归一化后宽度或高度小于 2%  
- **密集分布**：交通路口、人行道等场景中目标密集，遮挡严重  
- **多尺度混合**：同一张图像中存在近大远小现象

### 2.5 目录结构

```
dataset_visdrone/
├── data.yaml                    # YOLO 数据集配置文件
├── data_local.yaml              # 自动生成，含本机绝对路径
├── 数据集介绍.md                # 数据集说明文档
├── VisDrone2019-DET-train/
│   ├── images/                  # 训练图像
│   └── labels/                  # YOLO 格式标签 (.txt)
├── VisDrone2019-DET-val/
│   ├── images/
│   └── labels/
└── VisDrone2019-DET-test-dev/
    ├── images/
    └── labels/
```

### 2.6 标注格式

采用 **YOLO 标准格式**，每行一个目标：`<class_id> <x_center> <y_center> <width> <height>`，坐标均为归一化值 [0, 1]。

---

## 三、训练过程

### 3.1 训练脚本

训练脚本为项目根目录下的 `train_yolo11.py`，支持三种运行模式：

- **train**：训练模式（默认）；可加 `--resume` 从同目录下 `weights/last.pt` 续训  
- **val**：验证模式，需指定 `--weights`  
- **predict**：推理模式，需指定 `--weights` 与 `--source`（图片或视频路径）

依赖检查：运行前需已安装 **ultralytics**、**PyYAML**（`pyyaml`）。

### 3.2 基本流程

1. 检查 `dataset_visdrone/data.yaml` 是否存在  
2. 调用 `fix_data_yaml()`：读取 `data.yaml`，将 `path` 写为本机 `dataset_visdrone` 绝对路径，并生成 **`dataset_visdrone/data_local.yaml`** 供训练使用  
3. 加载 YOLO 预训练权重（`--resume` 时从 `{project}/{name}/weights/last.pt` 恢复）  
4. 调用 Ultralytics `model.train()` / `val()` / `predict()`  
5. 训练默认结果目录：**`runs/train/yolo11_visdrone/`**（由 `--project`、`--name` 决定；与代码中默认值一致）

> **与桌面应用默认路径的关系**：`detection_app/utils/storage.py` 中默认模型目录为 `runs/detect/runs/train/yolo11_visdrone/`。若你实际训练输出在 `runs/train/yolo11_visdrone/`，请在应用内 **「模型管理」** 中重新选择 `best.pt`，或修改 `detection_app/config.json` 的 `model_path` / `metrics_dir`。

### 3.3 使用示例

```bash
python train_yolo11.py                         # 默认配置训练（yolo11s，300 epoch 等）
python train_yolo11.py --resume              # 从 runs/train/yolo11_visdrone/weights/last.pt 续训
python train_yolo11.py --model yolo11m       # 指定模型大小
python train_yolo11.py --epochs 200          # 指定训练轮数
python train_yolo11.py --imgsz 1280          # 更大分辨率（小目标更佳）
python train_yolo11.py --multi_scale         # 开启多尺度训练
python train_yolo11.py --mode val --weights runs/train/yolo11_visdrone/weights/best.pt
python train_yolo11.py --mode predict --weights runs/train/yolo11_visdrone/weights/best.pt --source path/to/image.jpg
```

### 3.4 训练注意事项

- 图像尺寸建议 ≥ 640，推荐 1280（小目标分辨率更高）  
- batch 建议 16–32（显存不足时降低）  
- epochs 建议 200–300（VisDrone 收敛较慢）  
- 默认开启 mosaic 数据增强，最后 10 个 epoch 关闭以稳定收敛  
- 可使用 `--multi_scale` 多尺度训练提升小目标性能  

### 3.5 指标图表脚本（`generate_metrics_charts.py`）

根据训练产生的 **`results.csv`** 批量生成 **8 张** matplotlib 图表（与 Ultralytics 自带的 `results.png` 等互补），便于报告与答辩展示。

```bash
python generate_metrics_charts.py
python generate_metrics_charts.py --csv runs/train/yolo11_visdrone/results.csv --out runs/train/yolo11_visdrone/charts
```

默认读取：`runs/detect/runs/train/yolo11_visdrone/results.csv`（与 `storage.py` 默认 `metrics_dir` 一致）；若 `results.csv` 在 `runs/train/yolo11_visdrone/` 等路径，请用 `--csv` 指定。输出 8 张 PNG：`train_loss.png`、`val_loss.png`、`map_curves.png`、`precision_recall.png`、`learning_rate.png`、`combined_metrics.png`、`loss_comparison.png`、`training_time.png`。

---

## 四、训练参数

### 4.1 主要训练参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--model` | yolo11s | 模型：yolo11n/s/m/l/x |
| `--epochs` | 300 | 训练轮数 |
| `--imgsz` | 640 | 输入图像尺寸（小目标建议 1280） |
| `--batch` | 16 | 批大小 |
| `--lr0` | 0.01 | 初始学习率 |
| `--lrf` | 0.01 | 最终学习率比例 |
| `--patience` | 100 | Early stopping 等待轮数（**0** 表示关闭） |
| `--device` | "" | GPU 编号或 cpu |
| `--workers` | 4 | DataLoader 工作进程数 |
| `--project` | runs/train | 实验保存目录 |
| `--name` | yolo11_visdrone | 实验名称 |
| `--exist_ok` | False | 允许覆盖已有实验目录 |
| `--resume` | False | 从 `{project}/{name}/weights/last.pt` 续训 |
| `--save_period` | -1 | 每 N 个 epoch 额外存检查点（-1 表示仅保留 best/last） |
| `--seed` | 0 | 随机种子 |
| `--multi_scale` | False | 多尺度训练 |
| `--copy_paste` | 0.0 | Copy-paste 数据增强概率 |
| `--conf` | 0.25 | **predict** 模式置信度阈值 |
| `--source` | "" | **predict** 模式输入路径 |

### 4.2 模型与优化器

- **优化器**：SGD  
- **损失权重**：box=7.5, cls=0.5, dfl=1.5  
- **数据增强**：mosaic=1.0, randaugment, erasing=0.4, close_mosaic=10  

### 4.3 检测参数（应用配置）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `conf_threshold` | 0.25 | 置信度阈值 |
| `iou_threshold` | 0.70 | NMS IoU 阈值 |
| `max_det` | 300 | 单张图像最大检测数量 |

---

## 五、可视化图含义

训练完成后，Ultralytics 会在实验目录下生成以下可视化文件，用于分析模型表现：

### 5.1 训练过程图表

| 文件 | 含义 |
|------|------|
| `results.csv` | 每 epoch 的损失与指标（训练/验证 loss、mAP、精确率、召回率等） |
| `results.png` | 训练曲线概览，包含损失、mAP、学习率等随 epoch 变化 |
| `train_batch*.jpg` | 训练 batch 样本可视化，展示数据增强后的输入图像与标签 |
| `val_batch*_labels.jpg` | 验证 batch 的标签可视化 |
| `val_batch*_pred.jpg` | 验证 batch 的预测结果可视化 |

### 5.2 标签与类别分析

| 文件 | 含义 |
|------|------|
| `labels.jpg` | 标签分布图，展示各类别目标在图像中的位置与数量分布 |
| `labels_correlogram.jpg` | 标签关联图，展示类别间相关性 |

### 5.3 评估指标曲线

| 文件 | 含义 |
|------|------|
| `confusion_matrix_normalized.png` | 归一化混淆矩阵，展示各类别的预测与真实标签对应关系 |
| `P_curve.png` | 精确率–置信度曲线，随置信度阈值变化的精确率 |
| `R_curve.png` | 召回率–置信度曲线，随置信度阈值变化的召回率 |
| `F1_curve.png` | F1 分数–置信度曲线 |
| `PR_curve.png` | 精确率–召回率曲线（PR 曲线） |

### 5.4 脚本生成的补充图表（`generate_metrics_charts.py`）

在指定目录（默认与 `results.csv` 同目录，或由 `--out` 指定）生成与 Ultralytics 默认图风格不同的排版化图表，文件名见上文 **3.5 节**，便于插入 Word/PPT。

### 5.5 results.csv 主要列说明

`epoch`、`time`、`train/box_loss`、`train/cls_loss`、`train/dfl_loss`、`metrics/precision(B)`、`metrics/recall(B)`、`metrics/mAP50(B)`、`metrics/mAP50-95(B)`、`val/box_loss`、`val/cls_loss`、`val/dfl_loss`、`lr/pg0`、`lr/pg1`、`lr/pg2`

---

## 六、系统功能

本项目提供基于 PyQt6 的桌面应用 **「无人机航拍小目标检测系统」**，主要功能如下：

### 6.1 功能模块

| 模块 | 功能说明 |
|------|----------|
| **图片识别** | 支持 JPG/PNG/BMP 等格式，上传图片后点击开始检测，支持拖拽导入，可导出检测结果图片与 CSV |
| **视频识别** | 支持 MP4/AVI/MOV 等格式，加载视频后逐帧检测，显示进度 |
| **摄像头识别** | 实时调用本地摄像头进行目标检测 |
| **检测历史** | 查看所有历史检测记录，支持筛选与导出 |
| **模型管理** | 管理检测模型文件路径，调整置信度、IoU 阈值等检测参数，验证模型加载 |
| **指标展示** | 展示模型训练过程的各项可视化指标，包括训练曲线、指标汇总、可视化图表 |

### 6.2 入口与流程

- **入口**：`detection_app/main.py`，启动后进入登录窗口  
- **默认账号**：admin / admin123  
- **检测流程**：通过 `utils/detector.py` 中的 `ImageDetectWorker`、`VideoDetectWorker`、`CameraDetectWorker` 调用 Ultralytics YOLO 进行推理  

### 6.3 配置与存储

- **`detection_app/config.json`**：`model_path`、`metrics_dir`、`conf_threshold`、`iou_threshold`、`max_det`、`last_user` 等（首次运行可由程序按 `storage.py` 中 `DEFAULT_CONFIG` 生成）  
- **`detection_app/data/users.json`**：注册用户与密码哈希  
- **`detection_app/data/history.json`**：检测历史记录  

### 6.4 运行环境说明（Windows / Anaconda）

- 桌面端依赖 **PyQt6**；若在 **Anaconda base** 中通过 pip 安装较新的 **PyQt6-Qt6**，易与 conda 自带的 **Qt 6.9.x**（`Library\bin`）发生 **DLL 冲突**，表现为 `ImportError: DLL load failed ... QtWidgets`。  
- 代码已在 **`detection_app/main.py`** 开头对 Windows 做 **PATH、`QT_PLUGIN_PATH` 与 conda `Library\bin` 的协调**；`requirements.txt` 中将 PyQt6 约束为 **`>=6.4,<6.10`**，以降低与 conda Qt 6.9 混用概率。  
- 若仍无法启动，可在已激活的 conda 环境中运行 **`detection_app/fix_pyqt_conda.bat`**（卸载 pip 版 PyQt6 并改用 `conda-forge` 的 `pyqt6`），或安装 **VC++ 2015–2022 x64 可再发行组件**。  
- 安装依赖：`pip install -r detection_app/requirements.txt`（国内网络可使用清华 PyPI 镜像）。

---

## 七、项目技术栈

| 类别 | 技术 |
|------|------|
| **GUI 框架** | PyQt6 |
| **深度学习框架** | Ultralytics (YOLO11) |
| **检测模型** | YOLOv11 |
| **后端** | PyTorch (ultralytics) |
| **图像处理** | OpenCV, Pillow |
| **可视化** | matplotlib, pandas |
| **数据处理** | numpy, pandas |

### 依赖包（`detection_app/requirements.txt`）

```
# 与 Anaconda Qt 6.9.x 并存时避免 pip 升级到 6.11 引发 DLL 冲突；仍异常见 fix_pyqt_conda.bat
PyQt6>=6.4.0,<6.10
ultralytics>=8.0.0
opencv-python>=4.7.0
Pillow>=9.0.0
matplotlib>=3.7.0
numpy>=1.23.0
pandas>=1.5.0
```

训练与配置：`train_yolo11.py` 另需 **PyYAML**（`pip install pyyaml`）。`generate_metrics_charts.py` 使用 **matplotlib、pandas、numpy**（可与桌面端依赖一并安装）。

---

## 八、项目结构

以下为当前仓库主要目录与文件（根目录名称以你本机项目文件夹为准）。

```
项目根目录/
├── train_yolo11.py              # YOLO11 训练 / 验证 / 推理（--mode）
├── generate_metrics_charts.py   # 由 results.csv 生成 8 张指标图
├── 项目概述.md                  # 本文档
├── CLAUDE.md                    # 开发说明（若有）
│
├── dataset_visdrone/            # VisDrone 数据集
│   ├── data.yaml
│   ├── data_local.yaml          # 由 train_yolo11.fix_data_yaml 自动生成
│   ├── 数据集介绍.md
│   ├── VisDrone2019-DET-train/
│   ├── VisDrone2019-DET-val/
│   └── VisDrone2019-DET-test-dev/
│
├── detection_app/               # PyQt6 桌面检测系统
│   ├── main.py                  # 入口（含 Windows 下 Qt DLL 路径处理）
│   ├── login_window.py
│   ├── main_window.py
│   ├── fix_pyqt_conda.bat       # Anaconda 下 PyQt6 DLL 异常时的修复脚本
│   ├── config.json              # 运行期配置（可不存在，由程序生成默认）
│   ├── requirements.txt
│   ├── data/
│   │   ├── users.json
│   │   └── history.json
│   ├── pages/
│   │   ├── image_page.py
│   │   ├── video_page.py
│   │   ├── camera_page.py
│   │   ├── history_page.py
│   │   ├── model_page.py
│   │   └── metrics_page.py
│   └── utils/
│       ├── detector.py          # Image/Video/Camera 检测 Worker + YOLO 推理
│       ├── storage.py           # config / 用户 / 历史
│       └── styles.py
│
└── runs/                        # 训练与推理输出（路径随 train_yolo11 参数变化）
    └── train/                   # 默认 --project runs/train
        └── yolo11_visdrone/     # 默认 --name
            ├── args.yaml
            ├── results.csv
            ├── results.png
            ├── weights/
            │   ├── best.pt
            │   └── last.pt
            └── …                # Ultralytics 生成的 PR 曲线、混淆矩阵等

    # 若使用仓库内默认 config，应用可能指向 runs/detect/runs/train/yolo11_visdrone/
    # 请将 model_path、metrics_dir 与实际训练输出目录对齐
```

---

## 九、参考文献

如在论文中使用 VisDrone 数据集，建议引用：

```bibtex
@article{zhu2021detection,
  title={Detection and tracking meet drones challenge},
  author={Zhu, Pengfei and Wen, Longyin and Du, Dawei and Bian, Xiao and Fan, Heng and Hu, Qinghua and Ling, Haibin},
  journal={IEEE Transactions on Pattern Analysis and Machine Intelligence},
  volume={44},
  number={11},
  pages={7380--7399},
  year={2021},
  publisher={IEEE}
}
```

---

## 十、相关链接

- VisDrone 数据集官网：https://aiskyeye.com/
- VisDrone GitHub：https://github.com/VisDrone/VisDrone-Dataset
- ICCV 2019 挑战赛：Vision Meets Drone Object Detection in Image Challenge
